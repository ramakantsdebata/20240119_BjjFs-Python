# Immutable; Collection of keys
# Immutable collection of non-duplicate immutables

lst1 = [1, 2, 3, 4]
fset1 = frozenset(lst1)

# Supports all non-mutating operations that are supported on sets

fset2 = frozenset(fset1)